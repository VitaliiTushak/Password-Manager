<Page x:Class="PasswordManagerWPF.MVVM.View.Menu.Passwords.PasswordsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:passwordElements="clr-namespace:PasswordManagerWPF.MVVM.View.UserControls.PasswordElements"
      mc:Ignorable="d"
      HorizontalAlignment="Stretch"
      VerticalAlignment="Stretch">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <Button Grid.Row="0" 
                Margin="10" 
                Content="Add Password"
                Command="{Binding NavigateAddCategoryCommand}"></Button>
        
        <ItemsControl Grid.Row="1" ItemsSource="{Binding Passwords}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <passwordElements:PasswordElement DataContext="{Binding Password}"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Page>
