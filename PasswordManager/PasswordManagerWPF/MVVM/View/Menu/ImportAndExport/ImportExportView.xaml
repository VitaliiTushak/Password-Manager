<Page x:Class="PasswordManagerWPF.MVVM.View.Menu.ImportAndExport.ImportExportView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:PasswordManagerWPF.MVVM.View.Menu.ImportAndExport"
      mc:Ignorable="d"
      Title="ImportExportView">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Button Content="Import Passwords" 
                    Command="{Binding ImportPasswordsCommand}" 
                    Width="200" Height="50" Margin="10"/>
            <Button Content="Export Passwords" 
                    Command="{Binding ExportPasswordsCommand}" 
                    Width="200" Height="50" Margin="10"/>
        </StackPanel>
    </Grid>
</Page>
